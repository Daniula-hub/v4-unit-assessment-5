const initialState = {
    username: '',
    profile_pic: '',
  }

  const reducer = (state=initialState, action) => {
    switch(action.type) {
        case INCREMENT: {
            console.log({
             ...state,
  })
}


